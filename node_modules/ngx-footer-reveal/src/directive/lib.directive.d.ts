import { AfterViewInit, DoCheck, ElementRef, Renderer } from '@angular/core';
export declare class NgxFooterRevealDirective implements AfterViewInit, DoCheck {
    private el;
    private renderer;
    options?: any;
    shadow?: boolean;
    shadowOpacity?: number;
    zIndex?: number;
    height?: number;
    width?: number;
    private prev;
    private defaults;
    private settings;
    constructor(el: ElementRef, renderer: Renderer);
    private initialize();
    ngAfterViewInit(): void;
    ngDoCheck(): void;
    onWindowLoad(): void;
    private setMarginAndWidth();
}
